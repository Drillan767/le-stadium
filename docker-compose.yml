version: '3'
services:
  web:
    build: .
    env_file:
      - .env.docker
    links:
      - db
    ports:
      - '8000:80'
  db:
    image: mariadb:latest
    restart: always
    env_file:
      - .env.mysql
    ports:
      - 3306